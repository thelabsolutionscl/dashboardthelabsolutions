name: Auditoría Pedidos

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'js/**'
      - 'tests/pedidos-wiring.test.js'
      - 'tests/smoke.test.js'
      - '.github/workflows/pedidos-audit.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'js/**'
      - 'tests/pedidos-wiring.test.js'
      - 'tests/smoke.test.js'
      - '.github/workflows/pedidos-audit.yml'
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  pedidos-wiring:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validar sintaxis
        run: |
          node --check tests/pedidos-wiring.test.js
          for file in js/*.js; do node --check "$file"; done

      - name: Auditar sección Pedidos
        run: node --test tests/pedidos-wiring.test.js

      - name: Ejecutar smoke test general
        run: node tests/smoke.test.js
