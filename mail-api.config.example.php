<?php
/**
 * mail-api.config.php — configuración privada de mail-api.php
 *
 * COPIA este archivo como  mail-api.config.php  (SIN "example") y pega tu clave.
 * Súbelo JUNTO a mail-api.php en el servidor (mail-api.thelab.solutions).
 *
 * NO se versiona en git (está en .gitignore). Si alguien pide este archivo por
 * URL, PHP lo ejecuta y no muestra nada — la clave no queda expuesta.
 */
return [
    // API key de Resend (Dashboard de Resend → API Keys). Empieza con "re_".
    'RESEND_API_KEY' => 're_TU_CLAVE_AQUI',
];
